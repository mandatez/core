"""Framework integrations.

Each submodule imports its target framework at the top — install
MandateZ with the matching extra to pull those deps in::

    pip install "mandatez[langchain]"
    pip install "mandatez[crewai]"
"""
